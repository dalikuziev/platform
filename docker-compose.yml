services:
  api:
    build: .
    container_name: drf_api
    ports:
      - "8000:8000"
      - "4040:4040"
    env_file:
      - ./config/.env
    volumes:
      - .:/usr/src/app
      - ./static:/usr/src/app/static
      - ./media:/usr/src/app/media
    restart: always
    user: root